<h1>New Group</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.membership_organization_path(@conn, :create)) %>

<span><%= link gettext("Back"), to: Routes.page_path(@conn, :dashboard) %></span>
